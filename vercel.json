{
    "version": 2,
    "builds": [
      {
        "src": "app.js",
        "use": "@vercel/node",
        "config": {
          "timeout": 30 // Increased timeout to 30 seconds
        }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "app.js" }
    ]
  }